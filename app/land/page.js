import LandClient from './LandClient';

export const metadata = {
  title: 'Land Calculator — Noobsbot',
  description: 'Convert between Nepali land units: ropani, ana, paisa, dam and bigha, kattha, dhur.',
};

export default function LandPage() {
  return <LandClient />;
}
